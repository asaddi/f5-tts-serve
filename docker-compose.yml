name: kokoro-serve
services:
  server:
    build: .
    volumes:
      - ./data:/app/data
    ports:
      - "8000:8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
              count: all
